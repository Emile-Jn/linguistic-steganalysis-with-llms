# Connect to the cluster:
# 1. activate the TU VPN (Settings > VPN > VPN TU Wien)
# 2. Open terminal
# 3. enter ssh datalab

# Check current jobs:
squeue
# Check current node availability:
sinfo
# Check my running or finished jobs:
sacct
# Cancel my jobs:
scancel --user=e12229987


# Run classification:
sbatch run.sh test.py
# Request the a100:
sbatch --partition=GPU-a100 run.sh test.py -nmax 5
# Inference on stego dataset:
sbatch run.sh test.py --data-path qwen-stego
# Inference on gen cover inspired by stanford IMDB dataset:
sbatch run.sh test.py --data-path qwen3_stanford_imdb

# Evaluation:
python3 -m analysis.evaluation run_38

# Metrics:
python3 -m analysis.metrics run_38 --subset qwen3_stanford_imdb --positive-group gen-cover

# Obsolete options:

# Try other checkpoints:
sbatch run.sh test.py -nmax 5 --lora-path checkpoint-21000
# Request the a100:
sbatch --partition=GPU-a100 run.sh test.py -nmax 5
# Steganalysis only on cover texts:
sbatch run.sh test.py --cover-only


# Open the last output:
openlast